########################
# Cloud
########################
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic

cache:
  expiry-in-hours: 2

########################
# Match service
########################
match-api:
  port: 9090
  url: localhost:${match-api.port}